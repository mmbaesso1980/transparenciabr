{
  "name": "transparenciabr-functions",
  "private": true,
  "engines": {
    "node": "20"
  },
  "main": "index.js",
  "scripts": {
    "build": "node --check index.js && node --check src/radar/diarioScanner.js",
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "@google/generative-ai": "^0.21.0",
    "firebase-admin": "^12.6.0",
    "firebase-functions": "^6.1.0",
    "node-fetch": "^2.7.0",
    "stripe": "^17.4.0"
  }
}
