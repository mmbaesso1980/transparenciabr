fastapi>=0.115.0
uvicorn[standard]>=0.31.0
gunicorn>=21.2.0
pydantic>=2.9.0

# Google Cloud
google-cloud-bigquery>=3.26.0
google-cloud-firestore>=2.18.0
google-cloud-secret-manager>=2.20.0
google-cloud-pubsub>=2.25.0
google-cloud-storage>=2.18.0
google-auth>=2.35.0

# Firebase Admin (token verification + Admin SDK writes)
firebase-admin>=6.6.0

# HTTP / utils
httpx>=0.27.0
requests>=2.32.0
tenacity>=9.0.0

# Observabilidade
prometheus-client>=0.20.0

# Segurança / crypto (hash de CPF)
cryptography>=43.0.0

# Dev / linting (não instalar em produção)
# pytest>=8.3.0
# pytest-asyncio>=0.24.0
# httpx  (já listado acima, usado em testes)
