# Ocean Ways — Backend Dependencies
# Python 3.11+
# Consistente com a stack Cloud Run do TransparênciaBR

# === Web Framework ===
fastapi==0.111.0
uvicorn[standard]==0.29.0
python-multipart==0.0.9  # Form data parsing

# === Firebase / GCP ===
firebase-admin==6.5.0     # Firebase Auth JWT verification + Firestore
google-cloud-bigquery==3.21.0
google-cloud-pubsub==2.21.1
google-cloud-secret-manager==2.20.0

# === HTTP Client (async) ===
httpx==0.27.0             # Async HTTP client para chamar APIs externas
tenacity==8.3.0           # Retry com backoff exponencial

# === Validation ===
pydantic==2.7.1           # Request/response models
pydantic-settings==2.3.0  # Settings via env vars

# === Auth / Security ===
python-jose[cryptography]==3.3.0  # JWT utilities (complemento ao firebase-admin)

# === Utilities ===
python-dateutil==2.9.0    # Date parsing flexível
pytz==2024.1              # Timezone handling (America/Sao_Paulo)
structlog==24.1.0         # Structured logging (JSON) para Cloud Logging

# === Testing ===
pytest==8.2.0
pytest-asyncio==0.23.7
pytest-httpx==0.30.0      # Mock de chamadas httpx nos testes
httpx                      # Já acima — TestClient FastAPI

# === Dev only (não incluir na imagem de produção) ===
# black==24.4.2
# ruff==0.4.5
# mypy==1.10.0
